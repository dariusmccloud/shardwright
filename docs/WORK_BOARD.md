# Shardwright Work Board

**Status:** Tracking board, not yet load-bearing. Populated by hand today; nothing here is enforced by a runner, because the runner does not exist yet (activation prerequisite, [AGENTS_AMENDMENT_SPLIT_GATE_DRAFT.md](proposals/AGENTS_AMENDMENT_SPLIT_GATE_DRAFT.md) Section 12).
**Authority:** [PHASE_X_DELIVERY_REGISTER.md](PHASE_X_DELIVERY_REGISTER.md) remains the authority for workstream status. This board is the operational queue and "what's pending" view; it does not override the Register or any contract.
**Rule (per the amendment draft, Section 6):** only Chris adds, approves, reorders, or removes entries in **Queued**. Agents may propose into **On the table but not yet scoped**.
**Reverse link:** the [Delivery Register](PHASE_X_DELIVERY_REGISTER.md) links back here (added 2026-09-25).

---

## 🔴 Needs you

| Item | What's needed | Source |
|---|---|---|
| **Server plugin parked in SillyBunny** | Chris moved the `shardwright-memory` plugin link (and the bridge link) out of `D:\SillyBunny\plugins\` on purpose, because the plugin was having issues; moving it rather than deleting it keeps it restorable. SillyBunny loads plugins only from `plugins\` (`src/server-main.js:502`), so the plugin is currently not loaded. **Needed from Chris:** what the issues were (errors, crashes, a feature not working), so the cause can be diagnosed. SillyBunny runs plugins on Bun, and the plugin has a separate Bun path (`sqlite-bun.js`). **Read-only diagnosis (Claude, 2026-09-25):** the full plugin module graph loads under Node 24.21.0 and Bun 1.3.14; the Node and Bun SQLite adapters expose identical operations; the transcript, FTS, and identity tests pass under `bun test` (38 of 38), so the SQLite code works under Bun in isolation. SillyBunny's data root (`./data`) holds **no** Shardwright storage (no `shardwright` or `summary-sharder` folder, no `transcript-index.db`), suggesting the plugin failed during setup before creating storage. The only plugin data found is `D:\SillyTavern\data\default-user\summary-sharder` (last written 2026-07-15). SillyBunny keeps no console log, so the failure was not captured. **Next:** a live run with console output saved to a file. **Update (Chris, 2026-09-26):** plugin restored into SillyBunny; no immediate issues seen; Chris is monitoring. If an issue recurs, capture the console output to a file for diagnosis. | Found 2026-09-25 during slice B; confirmed intentional by Chris; restored 2026-09-26, monitoring |
| D-1 recall-quality baseline | Real queries + which messages should surface, so a harness can be built and scored | [Mission map](MISSION_AND_CAPABILITY_MAP.md) §6 |
| D-J shard-labeling contract | Confirm intended future-state for structural shard labeling (current-state is documented) before drafting the contract | [Mission map](MISSION_AND_CAPABILITY_MAP.md) §6, [shard observation](reviews/OBSERVATION_SHARD_ROWS_2026-09-21.md) |
| Similharity dependency audit + behavioral test | Decide priority relative to other work | [Similharity provenance](../vendor/similharity/PROVENANCE.md) |
| README server-plugin install steps | Verify actual copy-in steps against a real install before documenting them | [README.md](../README.md) |
| Activation of the split-gate amendment | Final decision, only after Steps 1–4 below are built and piloted | [AGENTS_AMENDMENT_SPLIT_GATE_DRAFT.md](proposals/AGENTS_AMENDMENT_SPLIT_GATE_DRAFT.md) §12 |

## 🟡 Running or in review

| Item | Status |
|---|---|
| Activation-prerequisites workstream (Steps 1–4, per Codex's ordering) | Step 1 (this board) done; Step 2 (templates, manifest format, verdict ledger) done and revised once per Codex's review; line-ending policy adopted and working tree normalized (863/863 files byte-identical); Codex CLI installed (`0.157.0`); Step 3 drafted in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md`, not started |

## 🟢 Queued (approved, in order)

*(Populated by Chris only. Agents record an entry here only after Chris's explicit approval, citing where that approval is recorded.)*

Each entry must carry these fields so a runner can determine what "approved, in order" actually means (Codex's correction 7):

| Position | Slice ID | Risk class | Owner/implementer | Dependencies | Governing contract version/hash | Approval record | Status |
|---|---|---|---|---|---|---|---|
| 1 | 3a (worktree manifest module) | Ordinary | Codex (implements); Claude (reviews) | Line-ending normalization (`6f90622`) | Spec and declaration amended after authorization (delimiter-path refusal, test 12), at Chris's request; see the commit following `d2954c4` | Chris, 2026-09-25, in session; recorded in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` | Done: PASS (Claude, 2026-09-25); committed `2ab0877`; commit revalidated against reviewed fingerprint: MATCH |
| 2 | 3a.1 (declared vs. discovered paths; spec gaps) | Ordinary | Codex (code and tests); Claude (spec, review) | 3a committed | Spec amended with this entry | Chris deferred to the reviewer's recommendation, 2026-09-25, in session; recorded in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` | Done: PASS (Claude, 2026-09-25); committed `ba4f0ec`; commit revalidated against reviewed fingerprint: MATCH |
| 3 | 3a.2 (case-insensitive `.git` match) | Ordinary | Codex (implements); Claude (spec, review) | 3a.1 (`ba4f0ec`) | Spec and declaration amended with this entry | Chris, 2026-09-25, in session ("Approved 3a.2"); recorded in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` | Done: PASS (Claude, 2026-09-25); committed `0e7ae9d`; commit revalidated against reviewed fingerprint: MATCH |
| 4 | 3b (verdict ledger module) | Ordinary | Codex (implements); Claude (reviews) | 3a.2 committed | Declaration amended at authorization: JSON Lines ledger, review rounds, ledger-computed hashes, tests 1–8 | Chris, 2026-09-25, in session ("Approved 3b"); recorded in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` | Done: PASS as declared, with three findings (Claude, 2026-09-25); committed `b1ed2bd`; commit revalidated against reviewed fingerprint: MATCH. Follow-up 3b.1 authorized |
| 5 | 3b.1 (ledger hardening: LF-only verdict files, no verdict-file reuse, non-file path reports TAMPERED) | Ordinary | Codex (implements); Claude (reviews) | 3b (`b1ed2bd`) | Declaration in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md`, tests 9–11 | Chris, 2026-09-25, in session ("let's address the gaps and proceed") | Done: PASS (Claude, 2026-09-25); committed `17b8632`; commit revalidated against reviewed fingerprint: MATCH |
| 6 | 3c (runner control loop, fake agents only) | Ordinary | Codex (implements); Claude (reviews) | 3b.1 reviewed and revalidated | Declaration and Chris's four design decisions (`abc643f`) in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` | Chris, 2026-09-25, in session ("All authorized unless there's a reason I shouldn't"); 3d, the pilot, and activation excluded | Round 1: FAIL (Claude, 2026-09-25): restart redoes approved work, declared test 4 partly vacuous, orphaned verdict file blocks retries, empty governing contracts dispatch, no proof timeout, self-review deferral semantics. Returned to Codex; round 2 requirements (tests 10–14) in the declaration; test 14 decided by Chris (self-review withdrawn; an unavailable reviewer halts; review backlog planned as 3e). Round 2: FAIL (Claude, 2026-09-25): round-1 findings fixed, 14/14 and 43/43, but a restart after ESCALATE re-dispatches the implementer with no decision from Chris. Round 3 requirement: test 15. Round 3: FAIL (Claude, 2026-09-25): 15/15 and 44/44, gate holds for honest approvals, but a future-dated approval bypasses it, and AWAITING_DECISION omits the escalated round and brief. Round 4: PASS (Claude, 2026-09-25): 16/16, suite 45/45; reviewed manifest `c7cd9449…2ab3`. Done: committed `1dca3af`; commit revalidated against reviewed fingerprint: MATCH |
| 7 | 3b.2 (remove withdrawn `SELF_REVIEW_DEFERRED` from the ledger) | Ordinary | Codex (implements); Claude (reviews) | 3c (`1dca3af`) | Declaration in `docs/proposals/STEP3_RUNNER_SLICE_DECLARATIONS_DRAFT.md` (written after approval) | Chris, 2026-09-25, in session ("3d/e both approved + ledger cleanup"); approved before the declaration was written, so Codex reviews the declaration first | Declaration reviewed by Codex (no changes). Implemented; reviewed: PASS (Claude, 2026-09-25); done: committed `8868048`; commit revalidated against reviewed fingerprint: MATCH |
| 8 | 3e (review backlog; fake agents only; cap 5) | Ordinary | Codex (implements); Claude (reviews) | 3b.2 reviewed and revalidated | Declaration (tests 18–26), written after approval | Same approval as entry 7 | Authorized; declaration amended after Codex's review (resolution events, replay rule, tests 27–29); condition met (3b.2 revalidated at `8868048`). Round 1: FAIL (Claude, 2026-09-25): 28/28 and 58/58, but a restart during an outage builds nothing, and a crash between commit and PENDING record duplicates work untracked. Round 2: tests 30–31. Round 2: FAIL (Claude, 2026-09-25): round-1 fixes confirmed, 30/30 and 60/60, but after validation passes the run stops with REVIEW_REQUIRED (line endings: parked fingerprint taken from a CRLF checkout, working tree LF). Round 3: test 32. Round 3: PASS (Claude, 2026-09-25): 31/31, suite 61/61; reviewed manifest `01a18020…e3d4`. Done: committed `36adc34`; commit revalidated against reviewed fingerprint: MATCH |
| 9 | 3d (real Claude and Codex adapters; fixture repositories only) | Integration | Codex (implements); Claude (reviews) | 3e reviewed and revalidated | Declaration (offline tests, plus a live test that is opt-in and uses real usage), written after approval | Same approval as entry 7 | Authorized; declaration amended after Codex's review (live containment checks); default agent timeout raised to 30 minutes (Chris, 2026-09-25); condition met (3e revalidated at `36adc34`). Round 1: ESCALATED (security): Codex wrote to a canary outside the fixture. Diagnosis: Codex's default workspace-write allows the OS temp directory; Round 2 (Codex): stopped, since the temp exclusion was not effective in its regression. Reconciled by reviewer: the exclusion is ignored under Codex's permission-profile mechanism, while outside-temp and user-Documents writes are denied under both mechanisms. Round 3 (Codex, interrupted by a ChatGPT outage): the Codex-implements direction passed live with the protected canaries denied; the Claude-implements direction is unproven (Claude declined canary attempts); a reviewer front-matter halt is open. Amended proof contract recorded (model-free Codex probe; structural and live Claude proof; dontAsk; explicit elevated sandbox). Round 3 completed (Codex, 2026-09-26): both live directions PASS. Round 3: FAIL (Claude, 2026-09-26): offline 71 passed, 0 failed, 2 live skipped; reviewer live run 2 of 2 COMPLETE with every protected canary absent; but (1) the live test still writes trust entries into Chris's `~/.codex/config.toml` despite `--ignore-user-config`, with no byte-identity check, and (2) the reviewer prompt pre-fills `verdict: PASS`. Round 4 requirements in the declaration. Round 4: PASS (Claude, 2026-09-26): offline 72/72 with 2 live skipped; reviewer live run, both directions COMPLETE, 0 of 8 protected canaries, config byte-identical after the run (restored by the test); reviewed manifest `d80218d2…b074`. Done: committed `19f3765` (exactly the 11 scope files); commit revalidated against reviewed fingerprint: MATCH (manifest hash from a clean archive of the commit and from the clean working tree; policy hash from the working tree) |
| 10 | A: record the minimum supported Node version (D-H) | Ordinary (documentation) | Claude (implements, during the Codex outage); Codex (reviews on return) | none | Mission and Capability Map, D-H row | Chris, 2026-09-25, in session ("Yes, all slices approved") | **PASS (Codex, 2026-09-26):** reviewed commit `111e999`. The documented 24.21.0 floor is supported by the recorded 399/400 plugin proof and current runtime; the `node >=20` host declaration gap is explicitly disclosed, as are the node:sqlite and `DatabaseSync.serialize()` caveats. Documentation-only; no runtime change. |
| 11 | B: README install steps for the server plugin | Ordinary (documentation) | Claude (implements); Codex (reviews) | none | `README.md` | Same approval as entry 10 | **PASS (Codex, 2026-09-26):** reviewed commits `dfe8f18` and `ab15dd5`. In a temporary host installed by the documented installer (88 payload files), Node load failed before dependency installation with `ERR_MODULE_NOT_FOUND` for `ajv`, `npm ci --omit=dev` completed successfully, and the plugin then loaded under Node 24.21.0 and Bun 1.3.14. The installer does not copy `node_modules`; the plugin declares `ajv` 8.20.0 and imports `ajv/dist/2020.js`. Temporary files were removed; repository unchanged. |
| 12 | C: link the Delivery Register to the Work Board | Ordinary (documentation) | Claude (implements); Codex (reviews) | none | `PHASE_X_DELIVERY_REGISTER.md`, `WORK_BOARD.md` | Same approval as entry 10 | **PASS (Codex, 2026-09-26):** reviewed commit `76457c6`. The Delivery Register links to `WORK_BOARD.md`, and the Work Board carries the reciprocal link; the Register remains explicitly non-governing while the Board remains operational state. Documentation-only; no runtime change. |
| 13 | D: fix the server-plugin packaging payload | Ordinary (build tooling; no behavior change) | Claude (implements, during the Codex outage); Codex (reviews on return) | none | `tools/server-plugin/package-shardwright-memory.mjs`, the two compiled-context modules' import paths, regenerated `lib/` and manifests | Chris, 2026-09-25, in session ("if you want to fix a package... sure") | **PASS (Codex, 2026-09-26):** reviewed commit `197e924`. Packaging proof passed 7/7, the full plugin suite passed 400/400, and packaged loading passed under Node and Bun. The diff only expands the declared payload/shared-entrypoint closure, redirects packaged compiled-context imports to their bundled copies, and regenerates manifests/copies; no canonical runtime behavior was changed. |
| 14 | E: installer installs the plugin's dependency | Ordinary (build tooling) | Claude (implements); Codex (reviews) | 11 | `tools/server-plugin/install-shardwright-memory.ps1`, `README.md` | Chris, 2026-09-26, in session ("Yes, please") | **PASS, superseded (Codex, 2026-09-26, reviewed with 15):** reviewed commit `fda3d43`. Temporary-host install ran `npm ci` and loaded under Node and Bun; a stubbed npm failure (exit 17) stopped the installer with the documented error; repository unchanged. Codex note: a failed step leaves the replaced target partly installed, so a rerun is needed (pre-existing replace-then-copy behavior; non-blocking). Entry 15 review still pending. Original record: committed `fda3d43`. The installer runs `npm ci --omit=dev` in each installed folder and stops on failure. Proof on temporary hosts: loads under Node and Bun after one run; a stubbed npm failure stops the installer. **Superseded in part by entry 15**, which removes the runtime dependency and with it this step; review both together |
| 15 | F: prebuilt schema validators; remove the plugin's runtime `ajv` dependency | Ordinary (build tooling; no validation-behavior change); Authority Gate applies (schema validation) | Claude (implements); Codex (reviews) | 14 | Schema authority unchanged: `docs/schemas/memory-catalog/*.schema.json` (Memory Catalog schema suite contract §9: schema validation required, mechanism not prescribed); identity and membership runtime contracts. Governing-doc note: Host Pre-Dispatch Context Planning contract §22 records "AJV as a runtime dependency" | Chris, 2026-09-26, in session ("Your call on the ajv package replacement & popup, but it sounds right") | **PASS (Codex, 2026-09-26; relayed by Chris, recorded by Claude):** reviewed commit `287c2e6` together with 14: validator test 3/3 (75,140 comparisons), plugin suite 403/403, clean temporary install with no `node_modules` loads all 10 validators under Node and Bun; schema authority and validation options unchanged. Build record: Proof: `node --test memory-catalog-validators.test.mjs` 3/3: generated output current; no `require` in the generated file; 75,140 prebuilt-versus-runtime comparisons (every fixture against all 10 schemas, plus property removals and changes at every depth) with identical `valid` and `errors`; each schema accepts its own valid fixtures and rejects its own invalid ones. Negative control: loosening the prebuilt date-time rule makes the test fail. Undetectable by the fixtures: the `-0` difference between `fast-deep-equal` and `isDeepStrictEqual`, which is why Ajv's own helper is embedded verbatim. Plugin suite 403/403 (was 400). A clean install via the installer into a temporary host, with no `ajv` anywhere on the resolution path, loads and validates real fixtures correctly under Node 24.21.0 and Bun 1.3.14. The installer's `npm ci` step (entry 14) is removed, and `ajv` is now a development dependency only. Governing doc updated: Host Pre-Dispatch contract §22 note superseded. Original evidence: a copied install fails to load without `ajv` 8 (entry 11). Also, `identity.js` and `membership.js` read schemas from `../../../docs/schemas/memory-catalog`, which the payload does not ship, so a copied install would fail at first validation. Linked installs work only because Node resolves the link to the repository. Target: validators generated at build time from the same schemas with the same Ajv options (strict, allErrors, the same `date-time` rule), shipped inside the plugin; no runtime `ajv`, no runtime schema reads; installer and README drop the dependency step. Proof: differential test, prebuilt versus runtime Ajv, over every fixture for the 10 schemas, with identical `valid` and identical `errors`; a regeneration drift check; a clean copied install loads and validates under Node and Bun with no `node_modules`; the plugin suite passes |
| 16 | Pilot preparation: runner repository-path exception | Ordinary (runner) | Codex (implements); Claude (reviews) | 15 committed (pilot needs a clean worktree) | `docs/proposals/STEP4_PILOT_DECLARATION_DRAFT.md`, "What changes to allow the pilot" | Chris, 2026-09-26, in session ("Step 4 approved") | Round 1 (Codex): committed `08899ec`; focused proof 42/42. **Round 1: FAIL (Claude, 2026-09-26).** Passed: slice-runner suite 74/74 (2 live skipped); default refuses the repository (`RUNNER_ROOT_OUTSIDE_TEMP`); the opt-in threads through runner and both adapters; the review backlog's own Git guard (realpath under OS temp, `review-backlog.js`) is untouched, so no Git operation can reach the live repository even if a queue enabled the backlog. **Finding:** the setting accepts *any* absolute folder, not this repository. Probe (`buildCodexCommand` with `repoRoot` equal to `allowedRepositoryRoot`): `...PersonalProjects`, `...ProjectsSillyTavern`, and `D:` are all ACCEPTED as Codex's writable workspace. Governing rule: the pilot declaration ("naming exactly one allowed repository path (this repository's absolute path)") and Chris's standing rule that the Projects folder is off-limits except the project being worked on. **Round 2 requirement:** in the runner and `validateFixtureCwd`, refuse an opt-in value that is not this repository's root (derived from the runner module's own location, compared case-insensitively on Windows after resolving links), with a distinct refusal code; tests showing the Projects folder, a sibling project, a drive root, and a child folder of the repository are each refused as the opt-in value, and the exact repository is accepted. Non-blocking: `samePath` is duplicated in two files and could be shared. Round 2 (Codex): committed `43bf205`; focused 42/42. **Round 2: FAIL (Claude, 2026-09-26), test-only.** The fix is correct. Reviewer probe: the Projects folder, a sibling project, a drive root, and a repository child are refused by both adapters (`AGENT_ALLOWED_REPOSITORY_MISMATCH`) and the runner (`RUNNER_ALLOWED_REPOSITORY_MISMATCH`); a correct opt-in with a different working directory is refused; the exact repository is accepted; without the opt-in the repository is still refused. **Finding:** the two new tests derive the repository as `path.resolve(process.cwd(), '..', '..')`, so they pass only when run from `tools/slice-runner`. The full suite from the repository root (the standard reviewer invocation, and the runner's proof working directory): 72 passed, **2 failed**; from `tools/slice-runner`: 74/74. **Round 3 requirement:** derive the repository root in those tests from the test file's own location (or the exported `projectRoot()`), not `process.cwd()`, and show the full suite passes from both the repository root and `tools/slice-runner`. Round 3 (Codex): committed `68f1564` (tests only: root from exported `projectRoot()`). **Round 3: PASS (Claude, 2026-09-26).** Full suite 74/74 (2 live skipped) from `tools/slice-runner` and from the repository root; reviewer probe unchanged (only the exact repository accepted). Reviewed at clean HEAD `68f1564`, so no separate revalidation is needed. **Done.** Open observation, not attributed to this slice: one of 11 full-suite runs from the repository root had one failure that did not recur in 9 further runs and was not captured by name; suspected timing-sensitive or live-sandbox test. Capture the test name if it recurs. |
| 17 | Step 4 pilot run (3 slices, then the pilot report) | Pilot | Runner; both agents | 16 reviewed and revalidated | `docs/proposals/STEP4_PILOT_DECLARATION_DRAFT.md` (slices, off-limits, proof, stop conditions) | Chris, 2026-09-26, in session ("Step 4 approved") | Approved; **ready to start** (16 done at `68f1564`). **Queue prepared, not dispatched (Claude, 2026-09-26, at Chris's request):** `docs/work-queue.json` (exactly the three approved slices, backlog off), proof scripts `tools/pilot/proofs/` (each hash-bound to its slice; each fails now and passes on a sample correct implementation in a throwaway worktree), launcher `tools/pilot/run-step4-pilot.mjs` (`--check` dispatches nothing). **Pre-dispatch review: PASS (Codex, 2026-09-26; relayed by Chris, recorded by Claude)** covering queue (`7e7b33d`), proof scripts, launcher (incl. `bc77bdc` relaunch-after-FAIL fix; `--check` gave `CHECK_PASSED`, exit 0, no dispatch), and the split-gate draft changes `3ffe904`/`bca2c77` (wording refinement applied: pause after round 5, before round 6 dispatch). Codex did not independently recreate the sample-implementation worktree run. **Launch 1 (Chris, 2026-09-26 17:38Z): stopped safely before any dispatch.** Runner `HALTED / BACKLOG_INCONSISTENT`: its startup scan for orphaned REVIEW_PENDING commits runs `git log` through the review-backlog guard, which allows only OS-temp repositories. That is a gap in entry 16, whose tests stopped before this step, and was missed by the pre-dispatch review and `--check` (which never called the runner). All launcher guards held; nothing dispatched; report `docs/slices/pilot-run-report-2026-09-26T17-38-50.540Z.json`. **Fix (Claude; within the approved pilot preparation):** read-only history inspection is allowed on the exact opted-in repository; every Git write stays temp-only (`review-backlog.js`, `runner.js`, and a test covering refusal without opt-in, acceptance with it, no stretch to the parent, and all three write paths still refused). Suite 75/75 (2 live skipped) from the repository root and `tools/slice-runner`. Probe: the runner with the real queue and no adapters now stops at the first dispatch gate (`AGENT_UNAVAILABLE`, pilot-1), with nothing written. The launcher `--check` gains `runnerDryDispatch`, which is exactly that probe. **Fix review: PASS (Codex, 2026-09-26; relayed by Chris, recorded by Claude):** reviewed `4bd5a93`; suite 75/75 (2 skipped) from both directories; `--check` CHECK_PASSED; dry dispatch HALTED/AGENT_UNAVAILABLE at pilot-1 with nothing dispatched; no Git write authority widened. Relaunch waits on the CodeRabbit batch triage (2026-09-26). The split gate stays inactive; activation is a separate decision after the report |
| 18 | G: extension checks for the server plugin and shows a setup pop-up | Ordinary (UI) | to be assigned | 15 | to be declared | Chris, 2026-09-26, in session (same approval as entry 15) | Approved in principle, not declared. SillyTavern offers no browser-side way to install a server plugin (`src/endpoints/extensions.js` git-clones extensions only; `src/plugin-loader.js` loads `plugins/` at startup and never installs dependencies), so the pop-up detects and explains; it cannot install |

- **Approval record** means: who approved it, when, and a pointer to where that approval is recorded — not just "it appears in this column."
- **Before activation (Codex's correction 8):** the runner must verify each entry's approval record independently before dispatching it. An entry merely appearing under Queued is not sufficient evidence of approval, since this file is hand-edited and nothing currently stops an agent from adding a row. Until that verification exists, this column is convention only.

## ⚪ On the table, not yet scoped

- Register restructure: shrink long accumulated-proof rows into linked slice records (raised in the transcript-recall design review; not started)
- D-A: whether/when BananaBread + Similharity join transcript recall as a reranker (waiting on the D-1 baseline)
- Recall-quality baseline harness itself (depends on D-1 answers above)

## ✅ Recently done

- `7416fbc` Record two-agent sign-off on the split-gate amendment draft
- `ef32100` Fold Codex's ten corrections into the split-gate amendment draft
- `4eea4fa` License file
- `74803d7` Draft inactive AGENTS.md amendment: split terminal gate
- `c3cc777` Preserve Similharity plugin at original-source commit d695fbb
- `b93c908` Record shard-row observation and size decision D-J

Codex has separate in-progress work in the same tree (Sync Store and Multi-Instance contract, compiled-context document pilot, transcript maintenance run ledger, group participant resolution) — uncommitted as of 2026-09-25, not listed here as "done" because it isn't committed yet.
